trigger:
- master
- dev/*

pool:
  vmImage: 'macos-latest'

variables:
- group: AgoraKeys

jobs:

- template: Android/build-template/build-android.yml
  parameters:
    project: 'Android'
    module: 'sample-custom-recorder'
    name: 'CustomRecorder'